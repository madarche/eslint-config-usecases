{
  "name": "eslint-config-usecases",
  "description": "ESLint configs by usecase",
  "version": "3.1.0",
  "author": "Marc-Aurèle DARCHE",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/madarche/eslint-config-usecases.git"
  },
  "keywords": [
    "eslint",
    "eslintconfig"
  ],
  "scripts": {
    "posttest": "npm run lint",
    "test:ci": "npm test",
    "lint": "eslint --ignore-pattern='!.*.js' .",
    "lint:fix": "eslint --fix --ignore-pattern='!.*.js' ."
  },
  "engines": {
    "node": ">=12.18"
  },
  "peerDependencies": {
    "eslint": ">=6.8.0"
  },
  "devDependencies": {
    "eslint": "7.4.0"
  }
}
